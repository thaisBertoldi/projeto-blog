<%- include ('../../partials/header-home.ejs') %>

<body>
  <div class="container">
    <% result.articles.rows.forEach(article => { %>
        <div class="card">
            <div class="card-header">
                <h2><%= article.title %></h2>
            </div>
            <div class="card-body">
                <a href="/<%= article.slug %>" class="btn btn-dark"> Ler artigo </a>
            </div>
        </div>
    <% }) %>
    <div class="row">
        <div class="col col-12">
            
            <% if (result.page > 1) { %>
                <a href="/articles/page/<%= result.page - 1 %>" style="float:left">< Página Anterior</a>
            <% } else if (result.page === 1) { %>
                <a href="/" style="float:left">< Página Anterior</a>
            <% } %>

            <% if (result.next) { %>
                <a href="/articles/page/<%= result.page + 1 %>" style="float:right">Próxima página ></a>
            <% } %>
        </div>
      </div>
  </div>
</body>

<%- include ('../../partials/footer.ejs') %>
